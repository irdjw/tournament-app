/**
 * Integration-test supabase stub.
 * Re-exports a real @supabase/supabase-js client configured from process.env,
 * which is populated from .env.test by the jest globalSetup script.
 */
export { supabase } from '../helpers/supabase-test-client'
